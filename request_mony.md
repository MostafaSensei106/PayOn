إليك شرح مفصل لـ **Request Money Flow** (طلب الأموال) بصيغة Markdown، مع التشديد على الحقول الإلزامية والملاحظات الهامة كما طلبت.

---

# 💸 Request Money Flow (طلب الأموال)

**Flow الـ Receiver والـ Sender**

> ℹ️ **قاعدة أساسية:** > * **الـ Sender:** هو الشخص اللي بطلب منه الفلوس (المُرسِل).
> * **الـ Receiver:** هو أنا / الشخص الذي يطلب الفلوس (المُستقبِل).
> 
> 

---

## 📥 أولاً: مسار مُستقبل الأموال (Flow of the Receiver)

هذا المسار يوضح الخطوات التي يقوم بها الشخص الذي **يطلب** الفلوس.

### 1️⃣ إنشاء مسودة المعاملة (Create Transaction Draft)

* **نوع العملية:** `POST` `CreateTransactionDraft`
* **الحالة:** Draft

**📌 الحقول الإلزامية المطلوبة (Required Fields):**

| الحقل (Field) | الوصف / القيمة المطلوبة ⚠️ |
| --- | --- |
| **`transactionTypeId`** | يجب أن يكون **`17`** (وهو الكود الخاص بـ Request Money). |
| **`senderId`** | الـ ID الخاص بالشخص الذي تطلب منه الفلوس (يتم إحضاره من `Check Wallet`). |
| **`receiverId`** | الـ Wallet ID الخاص بك (أنت). |

> ❌ **تنبيه هام جداً:** يجب إزالة حقل `paymentId` تماماً من الـ Request Body.

### 2️⃣ التحقق من الرقم السري للمحفظة (Check Wallet PIN)

* **نوع العملية:** `POST` `CheckWalletPin`
* **الحالة:** Verify

**📌 الحقول الإلزامية المطلوبة (Required Fields):**

| الحقل (Field) | الوصف / القيمة المطلوبة ⚠️ |
| --- | --- |
| **`draftIds`** | الـ ID الراجع من الـ Response في الخطوة الأولى (`CreateTransactionDraft`). |
| **`senderId`** | الـ ID الخاص بالشخص الذي تطلب منه الفلوس. |
| **`accountId`** | الـ Wallet ID الخاص بك (أنت). |
| **`pinHash`** | الـ PIN Hash الخاص بك. |
| **`transactionTypeId`** | يجب أن يكون **`17`**. |

✅ **النتيجة النهائية:** ظهور شاشة النجاح (Request sent successfully screen) وتأكيد إرسال الطلب بنجاح.

---

## 📤 ثانياً: مسار مُرسل الأموال (Flow of the Sender)

هذا المسار يوضح الخطوات التي يقوم بها الشخص الذي **طُلبت منه** الفلوس (للموافقة أو الرفض).

### 1️⃣ جلب المعاملات المعلقة (Get Pending Transactions)

* **نوع العملية:** `GET` `GetPendingTransactions`
* **الحالة:** Fetch
* **الوصف:** يقوم هذا الـ Endpoint بجلب كل الطلبات المعلقة (Requests) التي تم إرسالها لهذا المستخدم.
* **الهدف الإلزامي:** يجب الاحتفاظ بقيمة الـ **`draftId`** من هذه الخطوة لاستخدامه في الخطوات القادمة.

### 2️⃣ التحقق من الرقم السري للمحفظة (Check Wallet PIN)

* **نوع العملية:** `POST` `CheckWalletPin`
* **الحالة:** Verify

**📌 الحقول الإلزامية المطلوبة (Required Fields):**

| الحقل (Field) | الوصف / القيمة المطلوبة ⚠️ |
| --- | --- |
| **`accountId`** | الـ Wallet ID الخاص بالـ Sender. |
| **`pinHash`** | الـ PIN Hash الخاص بالـ Sender. |

### 3️⃣ التحقق عبر رمز الأمان (OTP Verification)

* **الحالة:** OTP
* **الوصف:** إدخال رمز الـ OTP المرسل للمستخدم (سواء عبر الموبايل أو الإيميل) للتحقق من هويته.

### 4️⃣ حفظ المعاملة (Save Transaction)

* **نوع العملية:** `POST` `SaveTransaction`
* **الحالة:** Confirm

**📌 الحقول الإلزامية المطلوبة (Required Fields):**

| الحقل (Field) | الوصف / القيمة المطلوبة ⚠️ |
| --- | --- |
| **`draftIds`** | يجب أن يُرسل كـ **Array** يحتوي على الـ `draftId` الأصلي بالإضافة إلى `draftId + 1`. |

> 💡 **مثال إلزامي للحقل `draftIds`:**
> إذا كان الـ `draftId` القادم من الخطوة الأولى هو `3`، فيجب أن تقوم بإرسال المصفوفة بهذا الشكل: `[3, 4]`.

### 5️⃣ تحديد حالة المعاملة (Set Transaction Status)

* **نوع العملية:** `POST` `SetTransactionStatus`
* **الحالة:** Final Step
* **الوصف:** هذه هي الخطوة الأخيرة في الـ Flow، وهي التي تحدد الحالة النهائية للـ Transaction بعد عملية الحفظ (Save).

**📌 الحقول الإلزامية المطلوبة (Required Fields):**

اي خطاء اور سلله تظهر لا تستخدم snack bars use dilogs context.dilog. and so on 

| الحقل (Field) | الوصف / القيمة المطلوبة ⚠️ |
| --- | --- |
| **`draftId`** | الـ `draftId` الأصلي القادم من خطوة `GetPendingTransactions`. |
| **`isApproved`** | **`true`** (في حالة قبول الطلب والدفع ✅) <br>

<br> **`false`** (في حالة رفض الطلب ❌). |

